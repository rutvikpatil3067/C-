#include<stdio.h>
#include<math.h>
int main()
{
	int num;
	int r;
	int sum=0;
	int temp;
	for(num=temp;num!=0;num=num/10)
	{
		r=num%10;
		sum=sum+(r*r*r);
}
	if(sum==num)
	
{
		
	printf("%d is an armstrong no",num);
}
	else
{
		
	
	printf("%d is not an armstrong no",temp);
}
return 0;
}
